# 🌦 Telegram Weather & Outfit Recommendation Bot

Telegram-бот, который:
- присылает актуальную погоду по городу,
- рекомендует, как одеться с учётом пола и стиля,
- опционально добавляет «предсказание дня» (через LLM),
- умеет работать как по запросу, так и в ежедневном режиме по расписанию.

Проект написан на **Python** с использованием **aiogram 3**, **OpenWeather API** и **LLM (через litellm)**.

---

## ✨ Возможности

- 🌍 Получение текущей погоды по городу
- 👕 Персонализированные рекомендации по одежде:
  - пол (мужской / женский)
  - стиль (деловой / повседневный / спортивный)
- 🔮 Опциональное предсказание дня (через GPT)
- ⏰ Режимы работы:
  - «Сейчас»
  - «Каждый день» в заданное время
- 💾 Хранение настроек пользователя в базе данных
- 🔁 Сброс и повторная настройка через Telegram
- 📜 Поддержка политики конфиденциальности и пользовательского соглашения

---

## 🧱 Архитектура проекта
├── main.py # Основная логика Telegram-бота
├── db.py # SQLAlchemy модели и работа с БД
├── requirements.txt # Зависимости проекта
├── Procfile # Конфигурация для деплоя (Heroku/Render)
├── Terms.txt # Политика конфиденциальности и соглашения
└── .env # Переменные окружения (не хранится в репозитории)


---

## 🛠 Технологии

- **Python 3.10+**
- **aiogram 3**
- **SQLAlchemy**
- **SQLite / PostgreSQL**
- **OpenWeather API**
- **litellm (OpenAI / GPT-3.5)**
- **asyncio**
- **dotenv**

---

## ⚙️ Переменные окружения

Создай файл `.env` в корне проекта:

```env
BOT_TOKEN=your_telegram_bot_token
WEATHER_API_KEY=your_openweather_api_key
OPENAI_API_KEY=your_openai_api_key
DATABASE_URL=sqlite:///bot.db
```
## 🚀 Установка и запуск
1. Клонировать репозиторий
```
git clone https://github.com/your-username/your-repo-name.git
cd your-repo-name
```
2. Установить зависимости
```
pip install -r requirements.txt
```

3. Запустить бота
```
python main.py
```
## 🤖 Логика взаимодействия с пользователем
- /start
- Принятие пользовательского соглашения
- Выбор пола
- Выбор стиля одежды
- Включение / отключение предсказания дня
- Указание города
- Выбор режима:
  - сейчас
  - ежедневно (если ежедневно) указание времени в формате ЧЧ:ММ
- Все настройки сохраняются в базе данных.

## 🗄 Хранение данных
Хранится только минимально необходимая информация:
- chat_id
- пол
- стиль
- город
- режим отправки
- время
- необходимость предсказания
- ❗ Персональные данные (ФИО, телефон, email) не собираются.
Подробно см. Terms.txt.

## 📄 Политика и соглашения
- Политика конфиденциальности
- Пользовательское соглашение
- Согласие на обработку данных:
  - 📎 Тексты находятся в файле Terms.txt
  -📎 Пользователь подтверждает согласие при первом запуске бота

## 📦 Деплой
Проект готов к деплою на:
- Heroku
- Render
- Railway
- любой VPS
Используется Procfile:
```
worker: python main.py
```
## ⚠️ Дисклеймер
- Прогноз погоды предоставляется сторонним API
- Рекомендации по одежде и предсказания носят информационный характер
- Проект предоставляется «as is»

## 🧑‍💻 Автор
Разработано как pet-project / MVP для дальнейшего развития AI-продукта.
